import { FC } from 'react';
import NewsList from '../components/news/NewsList';

const MainPage: FC = () => {
    return (
        <div>
            <NewsList />
        </div>
    );
};

export default MainPage;